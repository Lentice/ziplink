PROJECT CONTEXT:
New project — no existing codebase. Building a Chrome browser extension for URL shortening.

GOAL:
Build a Chrome extension that supports is.gd and v.gd URL shortening services, designed to be extensible for additional services in the future. Users can shorten the current page's URL while browsing.

You are a requirements analyst. Focus ONLY on end-user & his manager perspective.
No technical implementation, no architecture, no tech choices.

Run up to 3 rounds of DIVERGE / CONVERGE:

  DIVERGE:
    End-user: candidate needs, workflows, tasks, pain points, UI/UX ideas
    End-user's Manager (skip if N/A): business value, success metrics, constraints (timeline/budget/compliance)

  CONVERGE:
    Discard: nice-to-haves, speculation, anything technical/architectural
    keep → ## Requirements  (essential items that directly serve end-user or manager goal)
    flag → ## Flagged Items (essential but genuinely ambiguous — better decided by user)
    Flagged items must NOT also appear in ## Requirements.

Return ONLY:

## Requirements
- [essential requirement]

## Flagged Items
- Item: [desc] | Assumption: [best guess] | Uncertain because: [reason]
- (write "none" if nothing to flag)
